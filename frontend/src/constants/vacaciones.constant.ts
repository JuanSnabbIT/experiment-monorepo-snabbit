import { TColors } from '@/types/colors.type';

export const COLOR_ESTADO_VACACIONES: Record<string, TColors> = {
    '1': 'amber',    // Pendiente
    '2': 'emerald',  // Aprobado
    '3': 'red',      // Rechazado
};
